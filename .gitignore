*.db
*.db-journal
.cache/
.env

# Your own search config. These describe what you are looking for and which
# employers you are targeting, so they stay local; the *.example.yaml files are
# what ships, and `jobhunter init` copies them into place.
companies.yaml
profile.yaml

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/

# Exports are generated artefacts, not source.
*.xlsx
*.csv
!tests/fixtures/*.csv

.DS_Store
unresolved-companies.md

# Harvested ATS token lists. CC BY-NC 4.0 from Feashliaa/job-board-aggregator —
# free to use locally, not ours to redistribute, so they stay out of this repo.
data/
.harvest-state.jsonl
.harvest-cache/
